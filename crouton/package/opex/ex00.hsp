// ex00 : テキトーなサンプル

#include "opex.as"

//*
	// 短絡
	if ( short_and( 0, 0/0 ) ) {	// 左辺が false なので右辺は評価しない
		;
	}
	if ( short_or( 1, 0/0 ) ) {	// 左辺が true なので右辺は評価しない
		;
	}
	
	// 条件
	x = which( pred, vThen, vElse )	// 条件演算 pred ? vThen : vElse と同じ働き
	
	// 分岐
	repeat 7		// cnt 番目の曜日文字が返却↓
		it = what( cnt,  "日", "月", "火", "水", "木", "金", "土" )
		mes it
	loop
	
	// 交換
	swap a, b		// 変数 a, b の中身を交換
	
//*/
	stop
